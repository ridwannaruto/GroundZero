{% extends '::base.html.twig' %}

{% block title %}Volma - Projects{% endblock %}
{% block selectprojects %} class="active" {%endblock%}

{% block body %}
{% block admin %}
{% endblock %}
 <h2 class="price">Rate Volunteers</h2>

        {{ form_start(form) }}
           
    {# render the task's only field: description #}
    

    
   
        {# iterate over each existing tag and render its only field: name #}
        {% for tag in form.rates %}
           <section class="container">
            <div class="notify">
            {{ form_widget(tag.name) }}<br><br>{{ form_label(tag.rate) }}<br>{{ form_widget(tag.rate) }}<br>{{ form_label(tag.comment) }}<br>{{ form_widget(tag.comment) }}
        </div> </section>
      {% endfor %}
  
        <p align = "center"> 
    {{form_widget(form.submit)}}
{{ form_end(form) }}
            </p>
            <br><br>
           
   
    
{% endblock %}